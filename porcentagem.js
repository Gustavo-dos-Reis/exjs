const porcentagem = (nominador , denominador) => {
     var result = nominador /100 * denominador;  
    console.log (`${nominador}% de ${denominador}  é igual a ${result}`);
}

porcentagem ( 1, 40);